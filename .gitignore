# ── 依赖 ──
node_modules/

# ── 密钥 / 环境变量（务必不要提交！）──
.env
.env.*
!.env.example
*.local

# ── 构建产物 ──
dist/
build/

# ── 日志 ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── 临时 / 截图 ──
shots/

# ── 操作系统 / 编辑器 ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Claude 本地配置 ──
.claude/
