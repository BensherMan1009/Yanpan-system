# ── 依赖 ──
node_modules/

# ── 密钥 / 环境变量（务必不要提交！）──
.env
.env.*
!.env.example
*.local

# ── 构建产物 ──
dist/
build/

# ── 日志 ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── 临时 / 截图 ──
shots/

# ── 操作系统 / 编辑器 ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Claude 本地配置 ──
.claude/

# ── 参考资料 / 项目文档（仅本地保留，不纳入部署仓库）──
/*.mp4
/*.pdf
/*.docx
/图片*.png
/需要修改的问题截图/
